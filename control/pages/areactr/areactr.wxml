<!--pages/areactr/areactr.wxml-->
<view class='main'>
<view class='viewh1'> 
     <h1 class="h1">全部区域</h1>
     <view class="sxan">
     <image src="{{sx}}" bindtap='sxym' class="tp" mode="widthFix"></image>
      </view>
     <view wx:if="{{rommid == 'RS@xcx12'}}" class="tpbj">
      <image src="{{tp}}" bindtap='bindAdd' class="tp"  mode="widthFix"></image>
     </view>
     </view>
      <view class='flex-wrp_one'>
   <block  wx:for="{{sortedAreas}}" wx:for-item="item">
       <view class="info" data-id="{{item}}" id="{{index}}"   bindtap='areainfo'>
       <image src='/images/areactr/{{item.atImg}}'  class='tp1' style='width:100%; height:190rpx;' mode='widthFix'></image>
                <view class='areaname' wx:if="{{item.aiName==''}}">区域</view>
                <view class='areaname' wx:else>{{item.aiName}}</view>
      </view>
    </block>
</view>
</view>
<view>
    <loading hidden="{{hidden}}">加载中</loading>
</view>